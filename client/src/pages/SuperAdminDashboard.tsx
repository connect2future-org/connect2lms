import { useState } from "react";
import { Building2, Loader2, Plus, Search, ShieldCheck } from "lucide-react";
import { toast } from "sonner";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { BlueprintShell } from "@/components/BlueprintShell";
import { ActivityLogPanel } from "@/components/ActivityLogPanel";
import { trpc } from "@/lib/trpc";

export default function SuperAdminDashboard() {
  const utils = trpc.useUtils(); const [search, setSearch] = useState(""); const [showForm, setShowForm] = useState(false);
  const [form, setForm] = useState({ name: "", institutionType: "School", contactEmail: "", adminName: "", adminEmail: "", adminUsername: "", temporaryPassword: "" });
  const dashboard = trpc.platform.dashboard.useQuery(); const schools = trpc.platform.schools.list.useQuery(search ? { search } : undefined); const create = trpc.platform.schools.create.useMutation({ onSuccess: () => { toast.success("School provisioned with its administrator."); setShowForm(false); setForm({ name: "", institutionType: "School", contactEmail: "", adminName: "", adminEmail: "", adminUsername: "", temporaryPassword: "" }); void utils.platform.dashboard.invalidate(); void utils.platform.schools.list.invalidate(); }, onError: error => toast.error(error.message) });
  const metrics = (dashboard.data?.data as { metrics?: Record<string, number> } | undefined)?.metrics ?? {};
  return <BlueprintShell role="SUPER_ADMIN"><header className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="eyebrow">PLATFORM / MULTI-TENANT CONTROL</p><h1 className="mt-2 text-3xl font-semibold text-white">Institution registry</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-blue-100/70">Provision schools with isolated administrative boundaries and inspect platform-wide activity without trusting client-supplied tenant context.</p></div><Button onClick={() => setShowForm(value => !value)} className="bg-cyan-300 text-[#05205c] hover:bg-cyan-200"><Plus className="mr-2 size-4" />Provision school</Button></header>{showForm && <form onSubmit={event => { event.preventDefault(); create.mutate({ name: form.name, institutionType: form.institutionType, contactEmail: form.contactEmail || undefined, admin: { name: form.adminName, email: form.adminEmail, username: form.adminUsername, temporaryPassword: form.temporaryPassword } }); }} className="blueprint-panel mt-6 grid gap-3 p-5 md:grid-cols-2"><div className="md:col-span-2"><p className="eyebrow">NEW INSTITUTION / ADMIN BOOTSTRAP</p></div>{([["name", "Institution name"], ["institutionType", "Institution type"], ["contactEmail", "Contact email"], ["adminName", "Administrator name"], ["adminEmail", "Administrator email"], ["adminUsername", "Administrator username"], ["temporaryPassword", "Administrator temporary password"]] as const).map(([key, label]) => <label key={key} className="text-xs font-semibold text-blue-100/75">{label}<Input required value={form[key]} type={key === "temporaryPassword" ? "password" : "text"} onChange={event => setForm(current => ({ ...current, [key]: event.target.value }))} className="mt-1.5 border-white/20 bg-slate-950/40 text-white" /></label>)}<div className="md:col-span-2"><Button disabled={create.isPending} className="bg-cyan-300 text-[#05205c] hover:bg-cyan-200">{create.isPending && <Loader2 className="mr-2 size-4 animate-spin" />}Create isolated school</Button></div></form>}<section className="mt-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[["Schools", metrics.schools ?? 0], ["All identities", metrics.users ?? 0], ["Assessments", metrics.assessments ?? 0], ["Assignments", metrics.assignments ?? 0]].map(([label, value]) => <div key={String(label)} className="metric-card"><ShieldCheck className="size-5 text-cyan-300" /><span>{label}</span><strong>{value}</strong></div>)}</section><section className="blueprint-panel mt-5 p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="eyebrow">TENANT REGISTRY</p><h2 className="mt-1 text-xl font-semibold text-white">Provisioned institutions</h2></div><label className="relative"><Search className="absolute left-3 top-3 size-4 text-cyan-300" /><Input value={search} onChange={event => setSearch(event.target.value)} className="border-white/20 bg-slate-950/40 pl-9 text-white" placeholder="Search name or code" /></label></div>{schools.isLoading ? <p className="empty-state">Loading institutional boundaries…</p> : schools.data?.data.length ? <div className="mt-5 overflow-auto"><table className="technical-table"><thead><tr><th>Institution</th><th>Code</th><th>Type</th><th>Status</th><th>Provisioned</th></tr></thead><tbody>{schools.data.data.map(school => <tr key={school.id}><td className="font-semibold text-white">{school.name}</td><td className="font-mono text-cyan-200">{school.code}</td><td>{school.institutionType}</td><td><span className="technical-chip">{school.status}</span></td><td>{new Date(school.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div> : <p className="empty-state">No institutions match the current filter.</p>}</section><ActivityLogPanel /></BlueprintShell>;
}
